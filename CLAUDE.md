# DevAssemble Milestone Notes

Current implementation target: Milestone 1 (Skeleton).

Scope for this bootstrap pass:
- Set up the pnpm/Turborepo workspace.
- Implement shared contracts in `packages/types`.
- Create placeholder `core`, `providers`, and `web` packages.
- Create a CLI entry point in `apps/cli`.
- Add CI for lint, typecheck, build, and test.

Do not implement provider APIs, planner LLM calls, or executor state management in this milestone.

